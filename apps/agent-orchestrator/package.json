{
  "name": "agent-orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch & nodemon --watch dist --exec node dist/index.js",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-gateway/cache": "workspace:*",
    "@ai-gateway/core": "workspace:*",
    "@ai-gateway/logger": "workspace:*",
    "@ai-gateway/model-adapter": "workspace:*",
    "@ai-gateway/queue": "workspace:*",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.7.2"
  }
}
